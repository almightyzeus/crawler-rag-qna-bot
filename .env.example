# Copy this file to `.env` and fill in the values.

# Required for embeddings (and later, answer generation if you add it)
OPENAI_API_KEY=sk-...

# Optional: override the default embedding model
# EMBEDDING_MODEL=text-embedding-3-small

# Optional: override the default LLM model for answer generation
LLM_MODEL=gpt-4o-mini

# Test URL - Used by test scripts
TEST_URL=https://example.com/

# Test crawling parameters
TEST_MAX_PAGES=5
TEST_MAX_DEPTH=2